import React from 'react';
import { TableFooter as MuiTableFooter, TableFooterProps } from '@mui/material';

export const TableFooter: React.FC<TableFooterProps> = (props) => <MuiTableFooter {...props} />;

export default TableFooter; 